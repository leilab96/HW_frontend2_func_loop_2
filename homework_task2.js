// Second Task

//Számokat tartalmazó tömbön megy végig és összegzi az elemeket majd kiírja a változót

function sumArray(nums) {
  let sum = 0;
  for (let i = 0; i < nums.length; i++) {
    sum = sum + num[i];
  }
  console.log("Az összegük: " + sum);
}

const num = [4, 2, 2, 1, 1];
sumArray(num);
